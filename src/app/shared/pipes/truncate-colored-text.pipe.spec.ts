import { TruncateColoredTextPipe } from './truncate-colored-text.pipe';

describe('TruncateColoredTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateColoredTextPipe();
    expect(pipe).toBeTruthy();
  });
});
